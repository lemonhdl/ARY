# ARY GRS 002 Jumbotron — 作品说明文档

```text
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  项目名称    ARY GRS 002 Jumbotron                           │
│             DevCompass Racing 赛事现场大屏子系统              │
│                                                             │
│  一句话定位  一块由可信数据、可信赛道几何和可维护资产流程        │
│             驱动的赛事大屏，不靠写死坐标或静态贴图伪装。        │
│                                                             │
│  核心交付    · Jumbotron / Race Live View  公开赛事大屏       │
│             · Track Profile Calibrator    赛道校准工具       │
│             · track-runtime              共享位置计算引擎    │
│             · 三套 curated data profile   12/8/9 队         │
│             · 一条已确认赛道资产 + 三条候选赛道               │
│                                                             │
│  运行环境    Linux                                          │
│                                                             │
│  三大高光    · 赛程回放 — 13 帧全存档，不落下每个高光时刻       │
│             · 轨迹描线 — 鼠标沿底图描线，快慢先后一目了然       │
│             · 三级菜单 — hover/click 分级展示，不信息过载      │
│                                                             │
│  团队分工    王鹏粤（系统整合）· 王鹏杰（系统审核）             │
│             马浩洋（数字资产）· 蒋和岩（数据生成）             │
│             陆乐天（机动）                                   │
│                                                             │
│  代码规模    PoC-GRS-001/src/server.js  ~4889 行             │
│             验证通过  VERIFY_PASS × 17 轮迭代                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 一、赛题理解

### 1.1 GRS-002 的核心命题

ARY GRS 002：Jumbotron 是 DevCompass Racing（DCR）的赛事现场大屏子系统。评审标准的核心句阐明了它的命题：

> 一块赛事大屏可以由可信数据、可信赛道几何和可维护资产流程驱动，而不是靠手写位置、静态贴图或一次性动画伪装出来。

这个命题拆解为三个可独立验证的子命题：

1. **数据驱动**：Jumbotron 页面上的每一个展示元素——马匹位置、排名、进度、消息、风险——事实来源必须是 `RaceSnapshot`、`RacingEntrySnapshot`、`RidingMessageSnapshot` 和 `AttentionItem` 等结构化数据对象，不能以硬编码形式出现在 HTML 或 CSS 中。可验证性要求：切换 data profile 后页面内容应随之变化，而非保持固定。
2. **几何可信**：赛道底图只负责视觉皮肤。马匹位置的唯一事实来源是 `track.profile.json` 中描述的语义赛道几何——centerline 点集、path length、tangent/normal 方向、lane offset 偏移量。位置计算链路固定为 `roundProgress → s → sampled centerline point → tangent → normal → lane offset → HorsePose(x, y, rotation)`。可验证性要求：修改 `track.profile.json` 中的 centerline 点后，马匹位置应随之改变；仅修改底图图片则不应影响位置。
3. **资产可维护**：AI 生成或人工绘制的赛道底图，通过 Calibrator 校准为可运行的 `track.profile.json`，形成"底图 → 校准 → 验证 → 导出 → 运行时使用"的闭环，而非一次性的手工调整。

### 1.2 Jumbotron 的定位

Jumbotron 是**公开赛事大屏**，面向 Workshop / Hackathon / 路演现场的观众、组织者、教师和助教。它不是：

- 后台管理系统或完整排行榜页；
- Coding Agent Session 的全量回放工具；
- 多视图切换系统或独立第二大屏模式。

Jumbotron 展示摘要和赛事态势，不展示完整终端日志、Coding Agent Session、长文本评论流、复杂代码 diff 或未授权私密内容。

### 1.3 Jumbotron 要回答的五个现场问题

1. 当前比赛进行到哪里？（Header：LIVE 状态、Round / Phase、计时）
2. 谁领先，谁在追赶？（TOP3、赛道上的 Entry 位置与排名、gap label）
3. 哪些项目正在发生关键动态？（Riding Message 气泡、底部 ticker）
4. 整体进度和资源消耗是否健康？（KPI chips：完成度、活跃骑手、Tokens、CA 使用）
5. 哪些风险、阻碍或违规需要现场关注？（Attention Items：high/critical 高亮 + ticker 提醒）

### 1.4 目标用户

| 用户 | 关心什么 | Jumbotron 提供什么 |
|------|----------|-------------------|
| 现场观众 | 当前比赛发生了什么，谁领先，哪里有变化 | Race Live View、主赛道、TOP3、ticker、消息气泡 |
| 组织者 | 比赛整体是否健康，哪些队伍需要关注 | KPI、风险/阻碍/违规提醒、焦点详情 |
| 教师和助教 | 学生是否真的完成了可运行系统，而不是静态演示 | debug evidence、validation、data profile、Riding Record |
| 参赛团队 | 自己的项目在比赛中的公开状态 | Entry 标签、排名、进度、消息摘要 |

---

## 亮点一：赛程回放能力 — 关键信息全存档，不落下每一个高光时刻

Jumbotron 作为赛事现场大屏，核心场景是实时直播。但实际使用中存在两个延伸需求：晚进场的观众需要补看已经发生的赛程，组织者需要回溯某个时间节点各队伍的状态变化。为此，本作品在 Race Live View 的基础上额外实现了一套完整的**赛程回放系统**，由四个组件构成：

**13 帧三段式时序帧序列**。赛事数据不是单帧快照，而是一条覆盖起跑、比赛中、冲线三个阶段的完整时间线。起跑段各队 roundProgress 从近零起步；比赛中段覆盖追赶、反超、风险触发与状态切换；冲线段引入 finished 状态、名次锁定和冲线反超逻辑。每帧满足三个不变式：rank 为 1..12 的置换、每队进度逐帧单调不减、末帧 idx9 全字段严格等于当前直播帧 `race-snapshot.json`。共覆盖全部 9 种 motionState，19 个关键事件（含 overtake、lead_change、status_change、race_start、finish）。

**s 轴插值**。相邻关键帧之间不硬切翻页，而是在 `roundProgress` / `s` 轴上做平滑插值（当前 12 步/帧），每一步重新调用 track-runtime 的 `sampleInterpolatedPose` 沿中心线重新采样 HorsePose。这直接避免了在 x/y 坐标上做线性补间导致的弯道穿越问题——插值发生在赛道几何空间而非屏幕像素空间。回放速度可通过帧延迟参数调节。

**事件气泡层**。回放中 overtake（含被超越名单）、lead_change、status_change、finish 等事件以气泡形式渲染在赛道对应位置，带空间去重算法和入场/出场时序控制，避免事件密集时气泡堆叠。

**FIFO 双队列播报**。底部 ticker 采用 `readyQueue`（等待播报）和 `runningQueue`（前台滚动，上限 5 条）分离的架构。消息按绝对时间 FIFO 入队——更早发生的先进入 readyQueue。运行中的消息未完全移出屏幕前不被替换，新消息从右侧 append 入场。readyQueue 积压时滚动速度自适应加快。任意 ticker 消息可点击展开焦点详情。

这相当于在"直播大屏"之外，额外实现了一套**可回放、可复盘、可追溯的赛事时间线引擎**——现场观众不会因为晚进场而错过任何一个高光时刻。

---

## 亮点二：轨迹描线追踪系统 — 快慢先后，一目了然

赛道的语义几何（centerline）是 Jumbotron 全部位置计算的唯一事实来源——如果 centerline 不贴合底图道路，所有马匹的位置、朝向和气泡定位都会系统性偏移。评审标准要求 Calibrator 支持编辑 centerline points，但仅靠逐个手写 JSON 坐标数组，效率低且贴合精度难以保证。本作品在 Calibrator 中实现了一套**轨迹描线追踪系统**，将 centerline 的创建和校准变成可拖拽、可逆、可复核的操作：

**鼠标沿底图描线**。切换到描线模式后在赛道底图上拖动鼠标，系统以可配置的最小采样距离（默认 16px）自动沿轨迹采集中心线点，描线路径以虚线实时预览。描线完成后一键闭合——自动将首点复制到末尾作为闭合对偶点。描线过程支持撤销/重做（Ctrl+Z / Ctrl+Y），每次操作保存完整状态快照。

**拖拽微调与所见即所得**。描线完成后切换到中心线编辑模式，每个控制点可独立拖拽。关键设计：拖拽过程中马匹预览位置同步更新——Calibrator Preview 调用与 Jumbotron 完全一致的 `createJumbotronRuntime` 和 `sampleHorsePose` 函数。这意味着校准者在拖拽中心线时实时看到的是"大屏上马匹实际会跑到的位置"，而不是一套独立预览逻辑。同时支持画布点击添加新点、Shift+点击在最近线段插入点、反转路径方向。

**Progress Scrubber 进度可视化**。底部预览栏的滑条控制单马从 0% 到 100% 沿赛道移动，拖拽即见马匹在赛道各位置的实际姿态。多马预览模式支持 1-12 匹马同时展示，预置三种场景（聚集 clustered / 分散 spread / 冲刺 finish），用于验证 lane offset 分配后多马并行时分布是否合理、是否严重重叠。

**Safe Keypoint Mode 安全模式**。中心线几何确认后，如需微调检查点的进度位置（s 值），可切换到关键点安全模式——此模式下中心线控制点完全隐藏，只能拖动起点、终点和检查点手柄，手柄自动吸附到最近中心线位置。这为多人协作场景提供了保护：调整检查点进度时不会意外破坏已校准的几何。

从描线到拖拽到预览到校验，这套系统让"赛道底图 → 语义赛道"的校准过程变成了一条可视化、可追溯的生产线。

---

## 亮点三：多级菜单分级展示 — 不再信息过载，又不落任何细节

Jumbotron 作为公开大屏，面临一个信息密度矛盾：现场观众需要在 3 秒内看懂比赛态势，但组织者和教师同时需要能够深入任意队伍的完整摘要。将所有信息平铺到首屏会导致大屏不可读；首屏过于简略则无法支撑 drill-down。本作品设计了一套**三级信息深度**，在不同交互层级上分配不同信息密度：

**第一级：首屏态势（3 秒可读）**。观众第一眼看到的是赛道主舞台——马匹位置、排名标号、LIVE 状态、TOP3——以及贴边 KPI chips（完成度、在线骑手、Token、异常情况）。赛道占据首屏最大视觉面积，Header 和侧栏被压缩为辅助信息条。不堆文字、不列长表、不显内部状态。这一层回答"比赛在发生什么"。

**第二级：hover 浮层（1 秒悬停）**。鼠标悬停任何一匹马、一条消息气泡或一个风险标记，浮出紧凑的 tooltip——队伍名、排名、赛道进度、阶段进度、分数、状态、风险等级、CA 用量、最近消息摘要。信息比首屏丰富，但不抢占首屏空间；鼠标移开即消失。这一层回答"这个队伍现在是什么情况"。

**第三级：焦点详情（点击展开）**。点击任何一个赛道 Entry、消息气泡、ticker 条目或风险提醒，右侧焦点详情栏展开完整的结构化摘要——Entry 详情含排名/进度/阶段/分数/状态/风险/最近消息；Message 详情含类型/severity/来源/摘要/时间；Attention 详情含类别/严重程度/摘要/状态。所有信息锚定到数据来源，不展示 raw URL、完整 Session 或私密内容。这一层回答"关于这个事件/队伍，我需要知道的全部细节"。

**交互细节**：三级之间的过渡经过专门处理——hover 时 SVG 元素自动提升到绘制栈顶（`liftFocusSource()` 在 pointerover/focusin 时将目标追加到父节点末尾），保证 tooltip 不被相邻元素遮挡；焦点详情通过 CSS anchor target（`:target` 伪类）驱动展开与收起，不依赖额外的事件监听或状态管理；每个可交互元素通过 cursor change 和 focus ring 提供明确的 affordance。整套系统的信息分配原则是——首屏一眼扫全局，悬停一秒看概要，点击展开挖细节。

---

## 二、系统组成

### 2.1 整体架构

```text
DCR RaceSnapshot（公开赛事快照数据）
    │
    ▼
Jumbotron Adapter（适配层）
    │  将 RaceSnapshot 映射为：
    │  RacingEntrySnapshot[]、RidingMessageSnapshot[]、AttentionItem[]
    ▼
track-runtime（共享运行时）
    │  加载 track.profile.json
    │  采样 centerline、计算 tangent/normal
    │  应用 lane offset
    │  输出 HorsePose(x, y, rotation, laneId, state)
    ▼
Jumbotron / Race Live View（公开大屏）
    │  主赛道 SVG 渲染
    │  Header / KPI / TOP3 / ticker / 焦点详情
```

```text
AI / Design Track Background（设计时视觉底图）
    │
    ▼
Track Profile Calibrator（设计时校准工具）
    │  描线 / 编辑 centerline
    │  配置起点/终点、方向、lane offsets、checkpoints
    │  预览单马/多马运行
    │  Validate
    │  Export track.profile.json
    ▼
track-runtime → Jumbotron / Race Live View
```

### 2.2 子系统组成

```
Jumbotron Subsystem
├── Runtime Side（运行时）
│   ├── Jumbotron / Race Live View    — 公开赛事大屏页面
│   ├── Jumbotron Adapter             — DCR 数据 → runtime 输入的适配层
│   ├── track-runtime                 — 路径采样、HorsePose 计算
│   └── curated mock data             — 三套 data profile（full / smoke-8 / coverage-9）
│
├── Design-time Side（设计时）
│   ├── Track Profile Calibrator      — 赛道校准工具
│   ├── Track Profile Validator       — 赛道资产校验
│   └── AI-assisted Asset Pipeline    — AI 底图 → 人工校准的资产生产流程
│
└── Shared Assets / Contracts（共享资产与契约）
    ├── track-profile schema          — 赛道语义资产的数据规范
    ├── track.profile.json            — 运行时的事实来源
    ├── background.webp               — 视觉皮肤
    ├── preview.png                   — 带 overlay 的复核预览图
    ├── RacingEntrySnapshot contract  — Entry 数据契约
    └── RidingMessageSnapshot / AttentionItem
```

### 2.3 技术栈

- **运行环境**：Node.js（零外部依赖，仅使用 `node:http`、`node:fs`、`node:path`、`node:url` 内置模块）
- **部署方式**：单一 `server.js` 文件，同时承载公开页面、API 端点和静态资产服务
- **默认端口**：`4400`（可通过 `ARY_GRS001_PORT` 环境变量指定）
- **前端**：服务端渲染 HTML + 内联 CSS + 内联 SVG + 内联 JavaScript，无前端框架依赖

### 2.4 页面与接口

| 路径 | 用途 |
|------|------|
| `/jumbotron` | Race Live View 公开赛事大屏（默认） |
| `/jumbotron?profile=full` | 完整压力 profile（12 队） |
| `/jumbotron?profile=smoke-8` | 低负载视觉 smoke profile |
| `/jumbotron?profile=coverage-9` | 枚举覆盖 debug profile |
| `/jumbotron?debug=1` | Debug Mode（含 Calibrator 入口、Validation 面板、数据证据） |
| `/jumbotron/calibrator` | Track Profile Calibrator 设计时工具 |
| `/jumbotron/calibrator?candidate=real-explicit-closed-course` | Calibrator 加载第二赛道候选 |
| `/jumbotron/calibrator/keypoints?candidate=real-explicit-closed-course` | Safe Keypoint Mode（只调关键点 s 值） |
| `/jumbotron/calibrator?demo=1` | Calibrator 演示模式（含录制路径引导） |
| `/jumbotron/debug-preview.png` | 调试预览 PNG 导出 |
| `/api/jumbotron-data-evidence?profile=full` | 数据证据 JSON API |
| `/api/jumbotron-bubbles?profile=full` | 消息气泡轮询 API |
| `/api/jumbotron-replay?profile=full&frame=N` | 回放帧 API |

---

## 三、Jumbotron 信息架构

### 3.1 Race Live View 区域结构

```text
Race Live View（16:9 大屏）
├── Header / Status Bar
│   ├── 赛事标题（DevCompass Racing）
│   ├── LIVE 状态徽章
│   ├── 当前 Round / Phase
│   ├── 已用时间
│   └── 在线骑手数
│
├── KPI Chips（贴边状态条）
│   ├── 完成度
│   ├── 在线骑手
│   ├── Token 总量
│   ├── Claude 占比
│   └── 异常情况（风险 + 阻碍 + 违规合计）
│
├── Main Track Stage（主赛道舞台，占首屏最大视觉面积）
│   ├── 赛道底图（background.webp）
│   ├── 赛道几何 overlay（centerline path + track band）
│   ├── Horse Markers（骑手精灵图，含 motionState 动效）
│   ├── 排名标号（金色圆点，位于骑手头顶，不随动效晃动）
│   ├── 状态徽章（九态对应不同徽章图）
│   ├── Riding Message Bubbles（消息气泡，每 entry ≤1、全局 ≤3）
│   └── Checkpoint 标记
│
├── Left Side Rail（左侧辅助栏，与主赛道等高）
│   ├── TOP3（领先队伍 + gap label）
│   ├── Entry 图例（颜色 + 进度 + motionState + risk pill）
│   └── 风险/阻碍/违规提醒（high/critical 优先）
│
├── Bottom Ticker（底部现场播报）
│   ├── readyQueue（等待播报的消息队列）
│   ├── runningQueue（当前前台滚动消息，最多 5 条）
│   ├── FIFO 语义（更早发生的消息先进队列）
│   └── 点击消息展开焦点详情
│
├── Focus Detail Panel（焦点详情栏，CSS anchor target 驱动）
│   ├── Entry 详情：排名、进度、阶段、分数、状态、风险、最近消息
│   ├── Message 详情：类型、severity、entry、summary、时间
│   └── Attention 详情：category、severity、summary、status
│
└── Footer
    ├── LIVE Status
    ├── Theme
    ├── Organizer
    ├── Current Phase
    └── Next Phase
```

### 3.2 信息优先级

| 优先级 | 内容 | 说明 |
|--------|------|------|
| P0 必须可见 | LIVE 状态、当前 Round/阶段/计时、Race Live 主赛道、Entry 位置与排名、TOP3、风险/阻碍/违规提醒 | 首屏可见，无需交互 |
| P1 应该可见 | 完成度、总 Tokens、Codex/Claude 使用、在线/活跃骑手、Riding Message、Footer 状态 | 贴边或底部展示，不抢占主赛道 |
| P2 可增强 | 赛马动效差异、气泡避让、焦点详情 drill-down | hover/focus 延迟展示 |

### 3.3 公开展示边界

**应展示**：赛事标题/阶段/时间/LIVE 状态、Entry 排名/位置/进度/成本/风险/消息摘要、赛事级 KPI、实时 TOP3、Riding Message、Risk/Obstacle/Violation 提醒。

**不应展示**：完整终端日志、完整 Coding Agent Session、长文本评论流、复杂代码 diff、raw `targetUrl`、raw `remoteCockpitUrl`、本地绝对路径、未授权私密内容。

### 3.4 动画状态机

Jumbotron 支持 9 种 motionState，每种状态有独立的视觉表达：

| 状态 | 语义 | 展示方式 |
|------|------|----------|
| `idle` | 暂无明显推进 | 原地轻微待机（stay sprite，静止） |
| `running` | 正常推进 | 常规奔跑动效（run sprite） |
| `sprinting` | 近期推进明显 | 更快奔跑/高亮（run sprite + 加速动效） |
| `slowed` | 推进变慢 | 慢速移动/弱化（walk sprite） |
| `blocked` | 被阻碍或存在高风险 | 停顿/警示标记（walk sprite + alert ring） |
| `pit_stop` | 进入 Pit Stop | 停靠/维修提示（walk sprite） |
| `takeover` | 远程接管中 | 接管徽标（walk sprite + takeover badge） |
| `finished` | 当前阶段完成 | 冲线/完成标记（stay sprite + finished badge） |
| `stale` | 长时间无新数据 | 降低透明度/离线提示（超过 5 分钟阈值自动触发） |

### 3.5 消息降噪规则

- 每个 Racing Entry 同时最多展示 1 条气泡
- 全局同时最多展示 3 条气泡
- 风险、里程碑消息优先
- 普通消息进入底部 ticker
- 底部 ticker 采用 FIFO 队列语义（readyQueue → runningQueue）
- readyQueue 越长，滚动速度自适应加快

---

## 四、Calibrator 使用流程

### 4.1 Calibrator 定位

Track Profile Calibrator 是 Jumbotron 子系统的**设计时赛道校准工具**。它不是公开大屏的一部分，而是资产生产工具。它负责把 AI 生成或人工绘制的赛道视觉底图，校准为可被 track-runtime 稳定使用的 `track.profile.json` 语义赛道资产。

Calibrator Preview 与 Jumbotron 使用同一套 `track-runtime`（通过 `createJumbotronRuntime` / `sampleHorsePose`），保证"工具里看到的赛道预览"和"公开大屏里马匹运行的位置"不是两套逻辑。

### 4.2 页面结构

```text
Track Profile Calibrator
├── Top Toolbar
│   ├── Import Background        — 选择或输入赛道底图 URL
│   ├── Import Candidate Profile — 粘贴候选 TrackProfile JSON
│   ├── Validate                 — 运行校验
│   ├── Preview                  — 预览马匹位置
│   └── Export                   — 导出冻结 track.profile.json
│
├── Main Canvas（SVG 画布）
│   ├── Background Layer         — 赛道底图
│   ├── Centerline Layer         — 中心线（编辑态可拖拽控制点）
│   ├── Control Points Layer     — 可拖拽中心线控制点
│   ├── Lane Preview Layer       — 车道偏移虚线
│   ├── Checkpoint Layer         — 检查点标记
│   ├── Horse Preview Layer      — 单马/多马预览
│   └── Message Bubble Preview   — 气泡区域预览
│
├── Right Inspector
│   ├── Track Info               — 赛道名称、trackId、状态
│   ├── Geometry                 — centerline JSON 编辑、增删点、反转路径
│   ├── Start / Finish           — 起终点 s 值
│   ├── Direction                — 顺时针/逆时针 + 闭合开关
│   ├── Lanes                    — lane JSON 编辑 + count/spacing
│   ├── Checkpoints              — checkpoints JSON 编辑
│   ├── Message Bubble           — messageZones / noBubbleZones / riskZones
│   └── Validation Results       — 校验结果面板（pass/fail 逐项展示）
│
└── Bottom Preview Bar
    ├── Progress Scrubber         — 拖动滑条预览单马 0%→100%
    ├── Horse Count               — 多马预览数量（1-12）
    ├── Scenario Presets          — 聚集/分散/冲刺预设
    └── 导出调试预览 PNG          — 生成 debug-preview.png
```

### 4.3 完整校准流程

**Step 1：导入赛道底图**

打开 `/jumbotron/calibrator`，在 Top Toolbar 的 Import Background 下拉菜单中选择或直接输入赛道底图 URL（如 `/assets/jumbotron/background1.png`）。Main Canvas 左侧出现底图。

**Step 2：描线或导入中心线**

选择"描线"模式，沿底图赛道中心用鼠标描绘中心线（自动采样，min-distance 16px）。描完后可点击"闭合描线路径"自动闭合。或者直接在 Geometry 面板的 `<textarea>` 中粘贴或编辑 centerline points JSON。

**Step 3：编辑中心线控制点**

切换到"中心线"模式，Main Canvas 上出现可拖拽的控制点。拖拽点贴合底图赛道中心。支持：
- 画布点击添加新点
- Shift + 点击在最近线段插入点
- 点击单点删除按钮移除点
- 反转路径方向（一键）
- 切换闭合/开放路径
- 撤销/重做（Ctrl+Z / Ctrl+Y）

**Step 4：配置赛道参数**

在 Right Inspector 中设置：
- **Direction**：clockwise（顺时针）或 counterclockwise（逆时针）
- **Start / Finish**：起点和终点的 s 值（0-1），闭合赛道可合并为单个起终点标记
- **Lanes**：车道偏移 JSON 数组，或设置 lane count + spacing 自动生成。当前默认 12 车道
- **Checkpoints**：检查点 JSON 数组（含 s 值、label），或 Shift+点击画布添加

**Step 5：配置区域（可选）**

在 Message Bubble 面板编辑：
- `messageZones`：允许气泡出现的区域
- `noBubbleZones`：禁止气泡出现的区域（如 Header、Footer、KPI 区）
- `riskZones`：风险高亮区域

**Step 6：预览验证**

使用 Bottom Preview Bar：
- 拖动 Progress Scrubber（0%→100%），观察单马沿 centerline 移动是否贴合赛道
- 设置 Horse Count 和 Scenario Preset（clustered/spread/finish），验证多马 lane offset 分布
- 点击 Validate，查看校验结果面板：逐项显示 pass/fail，包括 centerline 点数、闭合一致性、lane offset 唯一性、checkpoint s 值范围、path length 最小阈值、曲率转角 warning、背景文件存在性、多马预览距离/重叠

**Step 7：导出**

点击 Top Toolbar 的 Export，页面底部展开导出面板。其中包含：
- 冻结的 `track.profile.json` candidate（完整的 JSON，可直接用于 Jumbotron runtime）
- JSON diff preview（导入配置 vs 导出候选的字段级差异对比）
- 资产审核状态（pending human review / rejected / confirmed）

**Step 8：确认资产**

资产目录中应有：
- `background.webp`：最终视觉底图
- `track.profile.json`：运行时事实来源
- `preview.png`：带 overlay 的复核预览图
- `asset-provenance.json`：资产来源记录
- `asset-validation.json`：资产验证记录

确认后的资产可被 Jumbotron 直接使用。

### 4.4 Safe Keypoint Mode（关键点安全模式）

对于已确认的中心线几何，如需微调起点、终点或检查点的 s 值而不破坏中心线，可使用 `/jumbotron/calibrator/keypoints`。此模式下中心线控制点不显示，只能拖动起终点和检查点手柄，调整后的值吸附到最近中心线位置。

### 4.5 Calibrator MVP 完成度

| 功能 | 状态 |
|------|------|
| 导入底图 | ✅ |
| 创建/编辑中心线（增删拖拽） | ✅ |
| Trace 描线（鼠标沿底图描绘） | ✅ |
| 闭合路径 / 反转方向 | ✅ |
| 设置起点/终点/方向 | ✅ |
| 配置 lane offsets | ✅ |
| 配置 checkpoints | ✅ |
| Scrubber 单马预览 0%→100% | ✅ |
| 多马预览（1-12 匹 + 场景预设） | ✅ |
| Validate（20+ 项检查） | ✅ |
| Export track.profile.json | ✅ |
| JSON diff preview（导入 vs 导出 14 字段对比） | ✅ |
| debug-preview.png 导出 | ✅ |
| messageZones / noBubbleZones / riskZones 编辑 | ✅ |
| 拖拽证明（before/current/delta SVG evidence） | ✅ |
| Safe Keypoint Mode | ✅ |
| Demo 模式 + 录制路径引导 | ✅ |

| P1 待完成项 | 状态 |
|-------------|------|
| zone 可视化画布编辑（非纯 JSON） | pending |
| AI 候选点自动导入 | pending |
| 自动检测尖角并修复 | pending |
| 自动分配 lanes | pending |

---

## 五、数据与资产说明

### 5.1 数据契约

#### TrackProfile（赛道语义资产）

```json
{
  "schemaVersion": "1.0.0",
  "trackId": "real-explicit-closed-course",
  "name": "Real Explicit Closed Course",
  "viewBox": { "x": 0, "y": 0, "width": 1672, "height": 941 },
  "background": { "src": "/assets/jumbotron/background1.png", "kind": "png" },
  "centerline": { "type": "polyline", "closed": true, "smoothing": "mvp-polyline", "points": [...] },
  "direction": "cw",
  "startFinish": { "startS": 0, "finishS": 1, "label": "终点线" },
  "lanes": [{ "laneId": "lane-1", "offset": -54 }, ...],
  "checkpoints": [{ "checkpointId": "cp-start", "label": "终点线", "s": 0.3125 }, ...],
  "messageZones": [...],
  "noBubbleZones": [...],
  "riskZones": [...]
}
```

#### RacingEntrySnapshot（运行时 Entry 快照）

| 字段 | 类型 | 说明 |
|------|------|------|
| `entryId` | string | Entry 唯一标识 |
| `riderName` | string | 骑手姓名 |
| `projectName` | string | 项目名称 |
| `caProvider` | "codex" \| "claude" \| "other" | CA 提供商 |
| `rank` | number | 当前排名 |
| `overallProgress` | number | 整体完成度（0-100） |
| `roundProgress` | number | 当前轮次进度（0-100，驱动赛道位置） |
| `phaseProgress` | number | 当前阶段进度（0-100） |
| `currentPhase` | string | 当前阶段（PRD/DEV/REL/OPS/PM） |
| `costTokens` | number | Token 消耗量 |
| `riskLevel` | "none" \| "low" \| "medium" \| "high" | 风险等级 |
| `obstacleCount` | number | 阻碍数量 |
| `violationCount` | number | 违规数量 |
| `motionState` | "idle" \| "running" \| "sprinting" \| ... \| "stale" | 动态状态 |
| `laneId` | string | 分配的车道 |
| `updatedAt` | number | 最后更新时间戳 |

#### HorsePose（track-runtime 输出的马匹姿态）

| 字段 | 类型 | 说明 |
|------|------|------|
| `entryId` | string | 关联 Entry |
| `x` | number | 画布 x 坐标 |
| `y` | number | 画布 y 坐标 |
| `rotation` | number | 朝向角度（度） |
| `s` | number | 沿中心线的归一化位置（0-1） |
| `laneId` | string | 所在车道 |
| `state` | string | 动态状态 |
| `zIndex` | number | 渲染层级 |

#### RidingMessageSnapshot

| 字段 | 类型 | 说明 |
|------|------|------|
| `messageId` | string | 消息唯一标识 |
| `entryId` | string | 关联 Entry |
| `source` | string | 消息来源 |
| `type` | "progress_update" \| "milestone" \| "risk_alert" \| ... | 消息类型 |
| `severity` | "low" \| "medium" \| "high" \| "critical" | 严重程度 |
| `summary` | string | 消息摘要 |
| `createdAt` | string | 创建时间 |
| `displayMode` | "bubble" \| "ticker" \| "both" | 展示方式 |

#### AttentionItem

| 字段 | 类型 | 说明 |
|------|------|------|
| `itemId` | string | 事项唯一标识 |
| `entryId` | string | 关联 Entry |
| `category` | "risk" \| "obstacle" \| "violation" | 事项类别 |
| `severity` | "low" \| "medium" \| "high" \| "critical" | 严重程度 |
| `summary` | string | 事项摘要 |
| `status` | "watching" \| "open" \| "resolved" | 事项状态 |
| `createdAt` | string | 创建时间 |

### 5.2 Data Profile 说明

当前提供三套 data profile，通过 `?profile=` 参数切换：

| Profile | Canonical ID | 条目数 | 用途 |
|---------|-------------|--------|------|
| `full` | `curated-full-12` | 12 队 | 完整压力测试，覆盖全部 9 种 motionState 和主要现场状态 |
| `smoke-8` | `smoke-8-visual-low-load` | 8 队 | 低负载视觉验证，适合录屏时先看清楚布局 |
| `coverage-9` | `coverage-9-enum-complete` | 9 队 | 枚举覆盖验证，适合验证状态机、风险边界和异常处理 |

非法 profile 参数不会 silent fallback，而会进入明确错误状态，避免 demo 看似正常但实际数据配错。

### 5.3 赛道资产

#### 已确认第二赛道：real-explicit-closed-course

资产目录：`Week2-Jumbotron/mock-data/raw/mock_data/tracks/real-explicit-closed-course/`

| 文件 | 说明 |
|------|------|
| `background.webp` | Jumbotron 大屏使用的赛道底图（503 KB） |
| `preview.png` | 带 centerline/zones/checkpoints overlay 的复核预览图（2.6 MB） |
| `track.profile.json` | 运行时语义赛道（40 个 centerline points、12 lanes、4 checkpoints、消息区/禁播区/风险区） |
| `asset-provenance.json` | 资产来源记录（AI 生成底图 → human trace → 人工确认） |
| `asset-validation.json` | 资产验证记录（43+ 校验项全部通过） |
| `human-traced-centerline.*` | 人工描线证据文件（preview.png、runtime profile、track.json、validation.json） |

资产状态：`humanReviewStatus: "confirmed"`（2026-06-13 人工确认）。

#### 候选赛道资产

另外三个候选赛道目录可用，均包含 track.profile.json、background.webp、preview.png 和资产记录：
- `bright-complex-closed-course`
- `explicit-closed-complex-course`
- `nonoval-curve-course`

### 5.4 视觉素材

| 素材 | 路径 | 说明 |
|------|------|------|
| 赛道底图 | `/assets/jumbotron/background1.png` | AI 生成的未来风白色赛道（1.9 MB） |
| 骑手-奔跑 | `/assets/jumbotron/rider3_run.png` | 全速奔跑状态（2.5 MB，1536×1024 RGBA） |
| 骑手-行走 | `/assets/jumbotron/rider3_walk.png` | 慢速行走状态（2.6 MB） |
| 骑手-静止 | `/assets/jumbotron/rider3_stay.png` | 原地待机状态（2.9 MB） |
| 状态徽章 ×9 | `/assets/jumbotron/state-badge-*.png` | 九种 motionState 对应徽章 |

---

## 六、PoC 边界与未实现部分

### 6.1 已实现（本次 PoC 范围）

- ✅ 可运行的 Jumbotron / Race Live View（`/jumbotron`），16:9 大屏布局
- ✅ Header（赛事标题、LIVE、Round/Phase、计时）、KPI chips、主赛道、TOP3、消息气泡、底部 ticker、风险提醒、Footer
- ✅ 数据驱动的位置计算：`roundProgress → s → centerline → tangent/normal → lane offset → HorsePose`
- ✅ 9 种 motionState（idle/running/sprinting/slowed/blocked/pit_stop/takeover/finished/stale）
- ✅ 消息降噪规则（每 entry ≤1 bubble、全局 ≤3、风险优先、ticker FIFO 队列）
- ✅ Focus Detail 焦点详情（点击 Entry/bubble/ticker/attention 展开）
- ✅ Debug Mode（`?debug=1`）：centerline/sampled points/lane offsets/checkpoints/collision boxes/stale entries
- ✅ Replay 回放（13 帧 timeline + s 轴插值 + event bubble）
- ✅ Track Profile Calibrator（四区布局、描线/拖点/JSON 编辑、单马/多马预览、Validate、Export、JSON diff、debug-preview.png）
- ✅ Calibrator Preview 复用 Jumbotron 同一套 track-runtime
- ✅ 三套 data profile（full 12 队 / smoke-8 / coverage-9）
- ✅ 1 条已确认赛道资产（real-explicit-closed-course）+ 3 条候选赛道
- ✅ 公开页信息边界：不展示 raw targetUrl/remoteCockpitUrl/本地路径/完整 Session/终端日志/复杂 diff
- ✅ 自动验证脚本（`npm --prefix PoC-GRS-001 run verify`）
- ✅ 项目 README + 三份核心文档 + review-ledger（per-line 追踪 + 45 截图目录）

### 6.2 未实现（PoC 边界之外）

本次 PoC 不声称完成完整 DevCompass Racing 平台。以下内容不在本轮范围内：

- ❌ 真实后端赛事聚合服务——当前使用 curated mock data
- ❌ 权限、房间和 Workshop 管理上下文
- ❌ 正式 Remote Racing Cockpit 协作流程
- ❌ 最终排名语义（当前为 mock 排名）
- ❌ 正式多赛道资产管理后台（当前为手动文件管理）
- ❌ 复杂 3D 或真实物理模拟