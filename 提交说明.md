# ARY GRS 001 提交

本提交围绕 GRS 001 的核心问题：

> Race 源数据留在 Organizer 侧，ARY 不持久化完整 Race 数据，仍能完成 Race 的创建、披露、组织与展示。

提交内容包括 PRD、可运行 PoC 和 Riding Record。

## 目录

```text
ARY-PRD/        PRD 提交稿
PoC-GRS-001/    可运行 PoC 和 PoC 说明文档
riding_record/  Agent Riding 过程记录和 UI 素材 prompt
```

## PoC 运行

进入 PoC 目录：

```bash
cd PoC-GRS-001
```

启动 ARY 页面服务：

```bash
npm start
```

另开终端启动 Organizer 本地评测服务：

```bash
cd PoC-GRS-001
npm run start:organizer
```

打开：

```text
http://127.0.0.1:4400
```

## PoC 说明

完整说明文档：

```text
PoC-GRS-001/PoC说明文档.md
```

其中包括：

- 使用说明
- 内容说明
- 实现说明
- 数据保存位置
- 对关键技术问题的逐项回答
- 当前完成度反思
- 未实现部分和后续加强方向

## 推荐演示路径

1. `/yard`：查看 ARY 当前可见的公开 Race。
2. `/race/grs-001`：查看 Organizer 主动披露的公开摘要和参与入口。
3. `/organizer/race`：查看 Race 创建记录、披露字段和生命周期控制。
4. `/organizer`：切断本地数据服务，模拟 Organizer 数据不可用。
5. `/team/submit`：在数据缺失时提交，页面不会生成评分。
6. 恢复服务后再次提交，进入 `/team/records` 和 `/team/replay` 查看整理后的回放记录。
7. `/leaderboard`：查看公开结果和六维结果画像。

## 验证

在 `PoC-GRS-001` 目录执行：

```bash
npm run verify
```

期望输出：

```text
VERIFY_PASS GRS001 creation disclosure lifecycle riding evidence result radar holds
```

## 文档位置

- `ARY-PRD/ary-grs-001-prd.md`：PRD 提交稿。
- `PoC-GRS-001/PoC说明文档.md`：PoC 使用、内容、实现和关键问题回答。
- `PoC-GRS-001/README.md`：PoC 快速运行入口。
- `riding_record/session-10-public-yard-private-source-restructure.md`：完整过程记录。
- `riding_record/artifact-ui-visual-prompts.md`：UI 视觉素材 prompt 和落地记录。
